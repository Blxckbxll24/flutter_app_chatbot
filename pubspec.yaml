name: flutter_frond
description: A new Flutter project

publish_to: 'none' # Remove this line if you plan to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '^3.9.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.7.0
  provider: ^6.1.2
  flutter_spinkit: ^5.2.0
  shared_preferences: ^2.2.2  # Para persistencia del historial
  flutter_dotenv: ^5.1.0      # Para configuración por entornos

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  
  assets:
    - .env.dev
    - .env.prod
